server {
    listen 80;
    server_name pasticceria.andreasalardi.it;

    # Redirect HTTP to HTTPS (uncomment after setting up Certbot)
    # return 301 https://$host$request_uri;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API Proxy
    location /api/ {
        proxy_pass http://backend:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Websockets support (if needed later)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Frontend Proxy
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS Block (Uncomment after obtaining certificates)
# server {
#     listen 443 ssl;
#     server_name pasticceria.andreasalardi.it;
#
#     ssl_certificate /etc/letsencrypt/live/pasticceria.andreasalardi.it/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pasticceria.andreasalardi.it/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://backend:8001;
#         ...
#     }
#     location / {
#         proxy_pass http://frontend:3000;
#         ...
#     }
# }
